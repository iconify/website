## SVG + CSS components for React {#svg-css-react}

Following a set of Vue and Svelte components, new easy to use icon components for React have been published.

See [SVG + CSS for React](/docs/usage/svg-css/react/index.md) for details.

There are over 200 icon component packages, one per icon set:

- `[npm]@iconify-react/material-symbols` for Material Symbols icons
- `[npm]@iconify-react/solar` for Solar icons
- `[npm]@iconify-react/tabler` for Tabler icons

... and so on.

To import an icon component, import default item from `[npm]@iconify-react/prefix/icon` where "prefix" is icon set prefix, "icon" is icon name.

Example:

```jsx
import GithubIcon from "@iconify-react/ri/github-line";

function GithubLink() {
  return (
    <a href="https://github.com/iconify">
      <GithubIcon height="1em" />
    </a>
  );
}
```

These packages will be automatically updated when Iconify icon sets are updated.
